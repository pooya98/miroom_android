<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miroom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_miroom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_miroom_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Book_page"></activity>
        <activity android:name=".Reservation_check" />
        <activity android:name=".Reservate" />
        <activity android:name=".Loading" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>